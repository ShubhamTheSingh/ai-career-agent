# Copy to .env for local runs. On Hugging Face Spaces, set these as Space secrets instead.
OPENAI_API_KEY=

# Optional — free lead notifications (e.g. an ntfy.sh topic URL like https://ntfy.sh/your-topic)
NOTIFY_WEBHOOK_URL=

# Optional persona overrides (defaults live in config.py)
# AGENT_NAME=
# AGENT_TITLE=
# AGENT_LINKEDIN=
